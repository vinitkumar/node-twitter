extends ../layouts/default

block content
  .well
    h3 #{title}
    if followers
      each follower in followers
          .well
              a.btn(href="/users/" + follower._id)= follower.username
    else
      h4 Seems like there is no one following you.