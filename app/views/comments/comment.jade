
if (comment && comment.user)
  .comment
    a(href="/users"+comment.user._id) #{req.user.name}
    | :&nbsp;
    != comment.body
    br
    .date= formatDate(tweet.createdAt, "%b %d, %Y at %I:%M %p")
